{
  "version": 2,
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install && cd MySiddur-blog-content && pnpm install && cd ..",
  "git": {
    "deploymentEnabled": {
      "main": true
    },
    "submodules": true
  },
  "crons": [
    {
      "path": "/api/cron/daily-update",
      "schedule": "0 23 * * *"
    }
  ]
}